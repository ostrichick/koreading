# 1–5 개선 작업 검증 기록 (2026-09-14)

현재 Warm Paper & Charcoal 테마, 실사/벡터 이미지 선택, 불규칙 용언 프롬프트를 유지했다. 이전 변경을 통째로 복원하지 않고 현재 코드에 적용했다.

## 구현
1. Cron은 CRON_SECRET의 정확한 Bearer 인증만 허용한다. AI 입력 스키마와 스트리밍 본문 24KB 제한, IP 분당 20/일 100 및 서버 일일 기본 1000회 제한을 추가했다. Redis 설정 시 공유 카운터를 사용하고 장애 시 요청을 거부한다. 미설정 시 인스턴스 메모리 제한이므로 배포 전체 할당량을 보장하지 않는다.
2. 리뷰와 합계는 같은 Firestore 트랜잭션으로 저장하며 UID 문서로 중복 집계를 방지한다. 규칙도 합계와 리뷰의 동시 변경을 검증한다. 탈퇴는 최근 인증 확인 → 삭제 표시 → 개인 학습 데이터 삭제/리뷰 익명화 → 프로필 → Auth 순서이며 실패 후 재시도할 수 있다.
3. 단어 캐시는 문맥과 언어를 포함하며 오래된 응답이 새 팝업을 덮지 않는다. AI 구조를 검증하고 레벨 테스트는 A1~C2 각 2문항을 요구한다. 해당 단계 두 문항을 모두 맞힌 레벨만 추천한다.
4. 공개 읽기 페이지는 실제 본문을 서버 렌더링하고 개별 canonical을 제공한다. 도서관은 서버에서 카드 메타데이터를 60초 캐시하고 필터/정렬 후 24개씩 보낸다. Firebase Admin 키나 새 복합 인덱스가 필요하지 않다. 현재 방식은 최대 45,000개 메타데이터를 읽으므로 대규모 서비스에서는 별도 검색 인덱스로 바꿔야 한다.
5. 회원/게스트의 본문, 튜터, 사전 조회를 공통 컴포넌트/훅으로 분리했다.

## 검증
- 단위 테스트 9개, Firestore 에뮬레이터 통합 테스트 5개, Chromium 화면 테스트 6개 통과.
- ESLint 및 프로덕션 빌드 통과. Edge Runtime 폐기 예정 경고는 남아 있다.
- 실제 로컬 서버 AI: 글 생성 HTTP 200(206자), 문맥 사전 HTTP 200(물었어요 → 묻다), 레벨 테스트 HTTP 200(A1~C2).
- 화면 테스트는 AI 응답을 모킹한다. 위 실제 API 검증은 별도로 수행했으며 생성물을 운영 DB에 저장하지 않았다.
- JavaScript 비활성 상태에서도 공개 글 본문과 canonical 확인.

## 운영 반영 및 제한
- 변경은 로컬 작업 트리에 있다. GitHub push, Vercel 배포, 운영 Firestore 규칙 배포는 이 작업에서 수행하지 않았다.
- 새 리뷰/탈퇴 동작에는 이 저장소의 firestore.rules 배포가 필요하다. 운영 데이터 백업과 대상 프로젝트 확인 후 규칙과 앱을 함께 반영한다.
- Cron 실행에는 CRON_SECRET을 설정해야 한다. Redis 공유 제한에는 UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN이 필요하다. AI_DAILY_REQUEST_LIMIT과 AI_RATE_LIMIT_SALT를 선택적으로 설정한다. 프록시의 전달 IP 헤더 신뢰도에도 제한이 의존한다.
- 과거 리뷰 중 userId가 없는 문서는 이름으로 소유자를 추측하지 않으며 자동 익명화 대상에서 제외된다. 삭제 표시는 재시도/동시 쓰기 차단을 위해 UID와 시작 시각을 유지한다. 익명 리뷰 문서 ID에도 기존 UID가 남는다. 완전한 식별자 제거에는 서버 마이그레이션과 별도 보존 정책이 필요하다.
- AI 교육 목표 분량/문법 커버리지는 이번 구조 검증과 다르다. 정상 짧은 글을 모두 거부하여 503을 유발하지 않도록 분량 목표를 강제하지 않았다. 교육적 품질은 추가 표본 평가가 필요하다.

## 재실행
npm run lint
npm test
npm run build
npm run test:browser
npm run test:emulators

Firestore 에뮬레이터는 Java 21 이상이 필요하다. 이 PC에서는 Android Studio/jbr를 JAVA_HOME으로 지정했다.
